import { DropdownHoverDirective } from './dropdown-hover.directive';

describe('DropdownHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownHoverDirective();
    expect(directive).toBeTruthy();
  });
});
